import 'package:flutter/material.dart';

class PregnantPage extends StatelessWidget {
  const PregnantPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 20),
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}
